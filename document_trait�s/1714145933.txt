taches(nom_tache,fichier_client,fichier_traité,#categorie_id)

categorie(libellé)

user_categorie(user_id,#categorie_id)

user(nom,prenom,age,telephone)


notifications(contenu,type)

notification_user(user_id,notifcation_id)


